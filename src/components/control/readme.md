# [Control](https://winduum.dev/docs/components/control.html)

## Installation
By default, we recommend copying and pasting the code from this directory to your project and remapping the imports to local.

```js
import { Control } from '@/components/ui/control'
```
### NPM
Alternatively, you can also import directly from `npm` so you can leverage updates.
```shell
npm i winduum-vue
```

```js
import { Control } from 'winduum-vue/src/components/control'
```

### Docs
Visit [docs](https://winduum.dev/docs/components/control.html) to learn more and see usage examples.
